{
  "name": "HistogramExample",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "sync": "rm -rf ./node_modules/react-native-histogoram; sane '/usr/bin/rsync -v -a --exclude .git --exclude example --exclude node_modules ../ ./node_modules/react-native-histogoram/' .. --glob='{**/*.json,**/*.js}'"
  },
  "dependencies": {
    "react-native": "^0.17.0",
    "react-native-histogram": "^0.2.0"
  }
}
